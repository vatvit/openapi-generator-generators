<?php

declare(strict_types=1);

/**
 * API Routes for {{appName}}
 *
 * Generated by php-adaptive generator from OpenAPI specification.
 *
 * Include this file in your Laravel routes/api.php:
 *
 * ```php
 * Route::prefix('api')->group(function () {
 *     require base_path('vendor/{package}/routes.php');
 * });
 * ```
 *
 * Or use Route::group with middleware:
 *
 * ```php
 * Route::group(['prefix' => 'v1', 'middleware' => ['api']], function () {
 *     require base_path('vendor/{package}/routes.php');
 * });
 * ```
 */

use Illuminate\Support\Facades\Route;

{{#apiInfo}}
{{#apis}}
// ============================================================================
// {{classname}} Routes
// ============================================================================

{{#operations}}
{{#operation}}
/**
 * {{httpMethod}} {{path}}
{{#summary}}
 * {{summary}}
{{/summary}}
 */
Route::match(['{{httpMethod}}'], '{{path}}', \{{controllerPackage}}\{{operationIdCamelCase}}Controller::class)
    ->name('{{operationId}}');

{{/operation}}
{{/operations}}
{{/apis}}
{{/apiInfo}}
